node_modules
dist
dist-ssr
*.local
.vite

# build outputs / packaged deliverables (use GitHub Releases instead)
/release
*.zip

# local editor/tool config (machine-specific paths)
.claude/

# Pages now deploys from CI (dist via Actions), not a committed folder
docs/
